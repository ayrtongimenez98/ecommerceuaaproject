@model IEnumerable<UAAEcommerce.Areas.Admin.Models.TipoCliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipcli_descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipcli_descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipoCliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipoCliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipoCliente })
        </td>
    </tr>
}

</table>
